# 阶段 1 验收记录

日期：2026-09-12。实现范围：结构化搜索、可用性检查、预约、快照、HTTP 入口及本地图片服务。

## 已完成

- 后端代码位于 `backend/src/borrowed_backend`，按 api/domain/data/tools 分层。
- 四个工具由单一 registry 定义；HTTP 复用工具实现。
- 预约请求补充城市和尺码，锁内读取最新商品并重新校验。
- 写入快照失败时回滚内存；幂等信息与预约一起持久化；损坏快照导致启动失败。
- 种子数据保持原样，代码注释及 docstring 为英文。

## 自动测试

命令：`.venv/bin/python -m pytest -q`

结果：**41 passed**。完整输出：[stage1-pytest.txt](stage1-pytest.txt)。

覆盖日期黄金案例与边界、首个失败原因、清洗期、配饰、种子加载及重复 ID、确定性搜索、软评分、公开字段、20 次并发预约、同键并发、等待锁之后重新读取商品、预约城市和尺码复检、幂等冲突、快照替换失败回滚、快照损坏、恢复后搜索与幂等、HTTP 错误、环境演示日期与领域依赖边界。

测试依赖产生两条弃用提示：Starlette TestClient 的 httpx 兼容层和 AnyIO BlockingPortal 别名。没有测试失败。本次验证的完整依赖版本记录在 `requirements.lock.txt`。

## 真实 HTTP 和进程重启

命令：`.venv/bin/python scripts/smoke_stage1.py`

结果：[stage1-smoke.json](stage1-smoke.json)。脚本启动真实 uvicorn 进程，使用本地 HTTP，退出后关闭进程并删除临时状态目录。

| 检查 | 结果 |
| --- | --- |
| 健康检查商品数 | 386 |
| 固定查询，预约前 | 22 件可借 |
| 20 次并发预约 | 1 成功、19 个 OVERLAPS_BOOKING |
| 预约后搜索 | 21 件可借 |
| 服务关闭后启动新进程 | 已有占用恢复 |
| 新进程重试同一幂等键 | 原预约、already_existed=true |
| 新进程搜索 | 已预约商品不出现 |
| CLI 日期覆盖环境日期 | 通过 |
| 本地商品图片请求 | 200 |
| catalog SHA-256 和 mtime | 前后不变 |

固定查询：演示日期 2026-09-16，穿着日 2026-09-18，默认 4 天，Hamburg，EU 38，dress，limit=1000。领域测试验证拒绝分布：城市 66、尺码 69、发货过晚 72、预约冲突 21。按确认结果采用 22 / 21，原规格的 24 / 19 未作为本轮断言，也未修改种子数据来满足旧数量。

## 边界

验证覆盖本机单实例服务和正常进程重启，以及模拟快照替换失败。没有验证断电恢复、跨主机运行、生产部署、浏览器页面、LLM 或 MCP；这些不属于阶段 1。并发测试使用独立幂等键，实际服务新增预约恰好一条。

本轮没有实现阶段 2/3，没有创建实际演示预约到默认状态目录。运行说明和可复制命令见 [README](../README.md)。
